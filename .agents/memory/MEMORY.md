# Memory Index

- [Metro JS bundle verification](metro-bundle-verification.md) — how to compile-check native-module features that can't run in the Replit sandbox (APK-only Unity/AdMob etc.).
- [Tool output identifier garbling](tool-output-quirks.md) — grep/ripgrep display can redact/alias identifiers; always re-read the real file to confirm names.
- [Expo native view import](expo-native-view-import.md) — `requireNativeView` is a re-export alias in `expo`; how local Expo modules expose native views in SDK 54.

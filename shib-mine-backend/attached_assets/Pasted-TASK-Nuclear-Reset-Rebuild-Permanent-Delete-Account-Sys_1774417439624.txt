TASK: Nuclear Reset - Rebuild "Permanent Delete Account" System from Scratch
"Hi Agent,

We are done trying to 'fix' the existing delete account code. It is filled with conflicts and constant 401/404 errors. I want you to PERMANENTLY DELETE all existing files, routes, and logic related to the current account deletion system. We are starting from ZERO.

NEW ARCHITECTURE REQUIRED: "Permanent Delete Account"

1. Clean Slate:

Remove all old references to the previous deletion flow in profile.tsx, AuthService, and the Backend.

Create a completely fresh component/logic named Permanent Delete Account.

2. The New Logic (Step-by-Step):

Trigger: User clicks on 'Permanent Delete Account' in the profile.

Step A (OTP Generation): When the user clicks 'Confirm & Send OTP', the app must generate a 6-digit OTP locally or via the server.

Step B (The Email): Use the following NEW Brevo Credentials to send the OTP. Do NOT use old environment variables. Hardcode these for this rebuild to ensure 100% connectivity:

SMTP Server: smtp-relay.brevo.com

Port: 587

Login: a52a39001@smtp-brevo.com

NEW SMTP KEY: xsmtpsib-57d87a3812ae04a7addce247e7bb94c093e2fbc9e18524fdd25eced8f3762011-Vw2ZH0wPTNPWBoby

Step C (Verification): A 6-digit input box must appear. Once the user enters the correct OTP:

The user's account must be permanently deleted from PocketBase.

The user's email must be instantly saved into the deleted_emails collection to prevent future re-registration.

The app must force-logout the user and return them to the Splash/Login screen.

3. Crucial Rules:

No Admin-Only Locks: Ensure the PocketBase API Rules allow a 'User' to trigger this deletion for their own record.

Build Stability: This new code must be written specifically to work on a Production APK build, not just a web preview.

Isolation: Do NOT touch the Claim logic, Leaderboard, or Login/Signup while performing this nuclear reset.

I have already verified this new SMTP key with a test email in the panel, and it is working. The failure is now purely in the code implementation. Rebuild it perfectly now."
CRITICAL: Mining Logic & Calculation Fix Required"While basic app functions are now stable, the core Mining Logic and Balance Calculations are critically flawed in the APK. I have analyzed the behavior, and you need to fix the following issues immediately without touching any other stable parts of the code:1. Reliable Mining & Server-Side Time Sync:The Issue: Even when users wait for a full real hour, the mining rewards are not being credited correctly to the account balance.The Fix: You must implement a strict Server-Side Timestamp logic.When a user clicks 'Start Mining', the app must fetch the current time from the server/PocketBase, NOT the user's phone.This 'Start Time' must be saved in the database.After 60 minutes, when the user clicks 'Claim', the app must again check the server time. If the difference is $\ge 60$ minutes, credit the reward.If a user tries to change their phone's manual time, the server check will fail, and the claim should be rejected. This will stop 'fake mining' while ensuring 'real mining' always works.2. Power Token Calculation Error:The Issue: The calculation for token deduction is wrong. For example, selecting the 10x Booster should deduct 15,000 PT (as set in Admin) + the base mining cost. However, it is only deducting around 2,100 PT.The Fix: Ensure the app pulls the exact 'Cost' values from the Admin Panel's booster settings. The deduction logic must be: (Base Cost + Booster Cost) = Total Deduction. Verify that the math is accurate before updating the database.3. Claim Button & Credit Popup Logic:Ensure that once the 60 minutes are up, the 'Claim' button triggers a reliable credit function.The success popup must show the exact amount credited, and this must reflect immediately in the SHIB Balance on both the Home and Wallet screens.4. UI/Layout Fix (Home Page Scrolling):The Issue: The AdMob banners at the bottom are covering part of the Home screen content.The Fix: Add extra Padding or Margin at the bottom of the Home screen ScrollView. The user must be able to scroll high enough so that all buttons/info are visible above the ad banner.STRICT RESTRICTION: * Do NOT change any code related to Sign-up, Forgot Password, or Account Deletion.Focus purely on the mining timer logic and the mathematical calculation of Power Tokens.Use Server-Time for all mining activities to prevent local device manipulation."
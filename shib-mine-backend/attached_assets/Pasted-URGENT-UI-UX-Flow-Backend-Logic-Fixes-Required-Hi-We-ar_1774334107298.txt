URGENT: UI/UX Flow & Backend Logic Fixes Required
"Hi,

We are in the final stages, but there are 3 critical issues and 1 platform query that need your immediate attention. I have attached the latest screenshots showing 401 errors and UI overlaps.

1. Delete Account - Email Delivery Failed (401): [Ref: Attached Screenshot]

Clicking 'Confirm & Send OTP' results in: Email delivery failed (401) - authentication not found in headers.

Issue: This means your Brevo REST API call is missing the API Key in the headers or the key format is incorrect.

Action: Re-verify the EXPO_PUBLIC_BREVO_API_KEY implementation. Ensure the request headers include SMTP key:  xsmtpsib-57d87a3812ae04a7addce247e7bb94c093e2fbc9e18524fdd25eced8f3762011-JyPzK9RXNM1BUrIj. The SMTP is working, so this is purely a REST API authentication fix in the code.

2. Refined Fraud Logic (Claim Button behavior): [Ref: Attached Screenshot]

Currently, the Claim button freezes or becomes unresponsive when a user manipulates the time. This is a bad user experience.

New Requirement: The 'Claim' button must ALWAYS be clickable.

The Flow: If a user manipulates time and clicks 'Claim', the button should trigger the action, but the server-side check must REJECT the tokens.

Instead of adding tokens, the app should show a 'Claim Rejected - Invalid Session' message and increment the fraud_attempts count. * The user should then be able to start a NEW mining session normally. Only after 3 such rejected attempts, the account should be blocked. Don't freeze the UI; just reject the transaction.

3. Major UI/UX Issue: Scrolling & Ad Overlaps: [Ref: Profile, Wallet, and Top Players Screenshots]

The current scroll view is broken. Components like 'Live Withdrawals', 'Withdrawal History', and the 'Delete Account' button are being hidden or cut off by the Test Ads at the bottom.

Action: You need to implement a proper ScrollView or FlatList with contentContainerStyle padding at the bottom. The entire screen content must be scrollable so that even the last element is fully visible above the ad banner. The user shouldn't have to struggle to see the 'Sign Out' or 'Delete' options.

4. Platform Query (Apple App Store):

Can we build this current codebase for the Apple App Store using Expo without any major code changes?

Is the current logic (PocketBase + Firebase + Game Canvas) compatible with iOS requirements, or will we need a separate architecture? Please provide a simple 'Yes/No' with a brief explanation of any necessary changes.

Note: I have provided screenshots for every issue mentioned. Please ensure the UI is fully scannable and the 401 error is resolved in the next build."
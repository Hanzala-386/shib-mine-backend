Agent, the logic you explained is still not working in the actual app. The 'Claim' button stays stuck, strikes are exceeding 3 without blocking, and the session is not resetting. Follow these strict technical instructions to fix it NOW:

1. Force Immediate Reset on Fraud:

Inside the claim function, the moment a fraud (time mismatch) is detected:

You must DELETE the current mining session record from the mining_sessions collection immediately.

Set the current_mining_session field in the users table to an empty string ("").

This is mandatory so the UI reflects that the mining has ended and the user must start over.

2. Strict Strike & Block Logic (Max 3):

In your code, use a strict condition: if (user.fraud_attempts >= 3).

If this condition is met:

Set user.status = 'blocked'.

The API response must return a specific error code (e.g., 403 Forbidden) with the message: 'ACCOUNT_BANNED'.

Frontend Action: When the frontend receives this ACCOUNT_BANNED error, show a Large Alert. Once the user clicks 'OK', call the signOut() function and redirect them to the Login screen immediately.

3. Fix the 'Claim' Button Loop:

The reason the 'Claim' button stays there is that the frontend state isn't being updated after the error.

Ensure that in the catch block of your handleClaim function:

You call setMiningSession(null) or your local state reset function.

This will force the UI to change from 'Claim' back to 'Start Mining' even if it was a fraud attempt.

4. Login Blockade:

Update the signIn / Auth logic. If a user tries to log in and their status == 'blocked', throw an error immediately: 'Your account is terminated due to fraud.' Do not let them enter the dashboard.

5. User Visibility:

Every time a strike happens (1 or 2), the popup MUST show the remaining attempts: "Strike {count}/3! Your progress is reset. 1 more attempt and you will be banned."

Check the PocketBase logs to ensure the fraud_attempts are not just incrementing in the background but are actually triggering these status changes.
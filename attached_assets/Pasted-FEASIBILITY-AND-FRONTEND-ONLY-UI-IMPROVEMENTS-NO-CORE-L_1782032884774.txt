FEASIBILITY AND FRONTEND-ONLY UI IMPROVEMENTS: NO CORE LOGIC CHANGES ALLOWED

Hey Agent,

Great news! The tournament system, database contract, and point calculation logic are now working perfectly. We do NOT want to modify any backend triggers, hooks, or core processing logic. This task is strictly focused on two premium UI/UX enhancements in the frontend application.

Please review these requirements and execute them cleanly inside the React Native / Expo components:

---

### UI TASK 1: PAST CYCLE LEADERBOARD DISPLAY DURING INACTIVE (RED) STATE
- CONTEXT: Currently, when a tournament ends and drops into the inactive (Red Dot) "Tournament will start soon" state, the leaderboard grid disappears or shows an empty placeholder.
- ENHANCEMENT: Instead of a blank screen, fetch and display the finalized rankings of the tournament cycle that just ended.
- REQUIREMENTS:
  1. Show the Top 3 winners prominently at the top, followed by the rest of the ranks up to Top 100 in the list view below.
  2. This historical leaderboard must remain visible ONLY while the current phase is inactive (Red state).
  3. The exact moment an admin launches a NEW active tournament cycle, this old history data must automatically swap out, and the UI must clear to show the fresh live cycle state (Green) as usual.

---

### UI TASK 2: WINNING REWARD CELEBRATION POPUP (PAMPHLET) WITH MANUAL CLAIM BUTTON
- CONTEXT: Currently, when a user wins a tournament, their reward amount is automatically credited directly to their wallet balance. We want to make this moment visually exciting for the user.
- ENHANCEMENT: Introduce a beautiful, clean modal/popup (like a winning pamphlet) that triggers when a winner opens the app after a cycle finalizes.
- REQUIREMENTS:
  1. Visuals: The popup must display a celebration layout with clear, bold text showing their exact winning amount (e.g., "Congratulations! You Won X SHIB!").
  2. Action: Place a prominent, stylized [CLAIM REWARD] action button directly inside this popup.
  3. UI-Logic Bind: The amount is already safely handled background-side, so clicking this button will simply execute the UI visual state shift (e.g., trigger the local balance sync/refresh animation, play a success effect, and dismiss the modal permanently for that cycle). It must not double-trigger any backend wallet adjustments.

---
Please implement these two premium frontend changes seamlessly. Ensure that style classes match our current dark theme and that the core tournament engine state remains untouched. Let me know once ready for review.
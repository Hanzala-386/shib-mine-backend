STRICT UI RE-LAYOUT AND DATABASE IMAGE UPLOAD MANDATE (DAILY REWARDS POPUP)

The server-side timing logic is now working perfectly. Do not touch or alter the timer engine. We now need to completely overhaul the UI layout of the 7-day grid boxes and fix the admin panel image database upload issue according to these exact visual design requirements.

1. GRID BOX NEW UI/UX STRUCTURAL LAYOUT:
   - Day Number Placement: The text indicating the day (e.g., "Day 1", "Day 2", "Day 3") must NOT be inside the card/box. It must be positioned strictly ABOVE each individual box. Make this Day text bolder, larger, and highly readable.
   - Reward Amount Placement: The reward token amount (e.g., "1K SHIB", "50 PT") must be positioned strictly BELOW each individual box.
   - The Box Content: The box card itself must be completely empty/clean inside so that it can elegantly display the 500x500 asset image fetched from the database without any text overlap.

2. FIXING THE POCKETBASE IMAGE UPLOAD ERROR (FOR THE AGENT TO EXECUTE):
   - We are currently experiencing an error when uploading files via the admin dashboard to the "daily_claim_settings" collection.
   - ACTION REQUIRED BY AGENT: You must programmatically or via backend scripts handle the storage/uploading of the reward images. 
   - I am providing a standard 500x500 pixel asset image. You must assign and map this asset image to ALL 7 dynamic day fields (`day_1_image` to `day_7_shiba_image` / `day_7_power_image`) in the database collection. Ensure any field type validation errors in PocketBase are handled and cleared immediately so that images render properly inside the newly emptied boxes.

3. VISUAL SUMMARY OF EACH CARD ELEMENT:
   [Day X Text - Bold/Top]
   -----------------------
   |                     |
   |  [500x500 Image]    |  <-- (The Box Card Container)
   |                     |
   -----------------------
   [Amount Text - Bottom]

Apply this layout update cleanly to all 7 days inside the modal component without breaking the server-time validation logic. Run a direct check on the PocketBase attachment schema to ensure the asset image loads perfectly.

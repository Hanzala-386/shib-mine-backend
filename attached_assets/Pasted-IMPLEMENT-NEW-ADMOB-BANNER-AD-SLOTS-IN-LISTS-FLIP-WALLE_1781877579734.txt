IMPLEMENT NEW ADMOB BANNER AD SLOTS IN LISTS & FLIP WALLET HISTORY LAYOUT

We need to implement two distinct layout modifications across the mobile application: adding structural dynamic Banner Ad placeholders inside existing lists/iterators and flipping the order of history modules on the Wallet screen. 

Please preserve all existing ad configurations exactly as they are. This task is strictly for adding new slots and shifting layout positions.

---

### PART 1: DYNAMIC BANNER AD INSERTIONS (EVERY 2 ITEMS IN ITERATORS)

Please inject Google AdMob / Unity Banner Ad containers dynamically into the following screen loops. Use an alternating injection algorithm so that a banner ad renders precisely between every two sequential items (Item 1 -> Item 2 -> BANNER AD -> Item 3 -> Item 4 -> BANNER AD, etc.):

1. TASK SCREEN PAGE:
- Locate the main dynamic loop component where user tasks are rendered.
- Programmatically insert a Banner Ad component between every 2 tasks. For example, if there are 3 tasks active, render an ad exactly between Task 1 and Task 2, and another ad between Task 2 and Task 3.

2. VIP LEVELS TIER PAGE:
- In the dedicated VIP Levels screen where tiers (VIP 1 to VIP 8) and their requirement details are listed, inject a structural Banner Ad slot between every 2 VIP card entries.

3. WALLET HISTORICAL LOGS LIST:
- Inside the Wallet screen's bottom historical list/scroll wrapper, inject a recurring Banner Ad container between every 2 log entries.

---

### PART 2: WALLET HISTORY SECTIONS RE-ORDER (FLIP COMPONENT)

We need to reorganize the vertical hierarchy of the transaction history widgets located inside the main Wallet screen layout context. 

- CURRENT STRUCTURAL HIERARCHY: Mining Session History is mounted on TOP, and Withdrawal Status History is mounted directly BELOW it.
- NEW FLIPPED HIERARCHY REQUIREMENT: Please invert this ordering explicitly. Mount the Withdrawal Status History component on the absolute TOP layer of the logs area, and push the Mining Session History component directly BELOW the withdrawal section.
- CRITICAL: Do NOT alter, modify, or refactor any backend controllers, local storage hooks, or state management fetching logic for either module. Only modify the JSX/TSX rendering stack order to achieve this visual inversion.

---

Execute these UI updates and test the list injection algorithms to ensure clean conditional rendering across all target list views. Inform me once compiled successfully.
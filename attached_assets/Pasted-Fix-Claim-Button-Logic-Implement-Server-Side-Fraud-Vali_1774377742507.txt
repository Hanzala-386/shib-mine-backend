Fix Claim Button Logic & Implement Server-Side Fraud Validation
"Hi Agent,

We are still facing a critical issue with the Claim Button that hasn't been resolved after 7-8 attempts. Since I cannot attach a video, I am describing the exact behavior shown in my testing. Please read this carefully and fix the core logic.

Current Problem (As seen in testing):
When the mining session is complete and I click the 'Claim' button, it changes to 'Claiming...' and then stays stuck there. Even with your '8-second race timeout' fix, the user experience is broken. The button does not complete the action, and the user is forced to wait or restart. This is unacceptable for a production app.

The Logic I Need (Step-by-Step):

Button Interaction: As soon as the user clicks 'Claim', the app MUST call the backend (Express/PocketBase).

Server-Side Time Validation (Crucial): * The server must calculate the time difference based on the Server's Clock, not the user's phone.

If the time elapsed since the last claim is less than 1 hour (Fraud attempt), the server should Reject the claim.

The Response:

If Valid: Add the SHIB tokens to the user's balance and show a 'Success' message.

If Fraud (Time manipulation): The server must send a 'Rejected' status. The app should then show a popup: "Claim Rejected: Time manipulation detected. No tokens added."

Outcome: In case of rejection, 0 tokens should be credited, and the mining session should reset. The user must NOT be blocked immediately, but the fraud_attempts count in the database should increase.

Why your current '8-second fix' is not enough:
Your fix just prevents the app from hanging forever, but it doesn't solve the fact that the server is not responding correctly to the claim request. We need the API to respond instantly with either a 'Success' or a 'Reject' based on the 1-hour rule.

Action Required:
Rewrite the handleClaim function to ensure that the UI always resets and provides feedback (Success or Rejection) within 1-2 seconds. Use the server time for validation so that users changing their phone's clock cannot cheat.

Please fix this first. I will provide the next task only after this is working perfectly.
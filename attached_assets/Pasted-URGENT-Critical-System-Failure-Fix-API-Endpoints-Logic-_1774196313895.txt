URGENT: Critical System Failure - Fix API Endpoints & Logic Mismatches
"Hi,

I have verified my PocketBase SMTP settings, and they are working perfectly (Test email successful). The issue is NOT on my server side. The constant HTTP 404 errors and the failure of Score-to-Token conversion prove that your code is pointing to incorrect or local API endpoints.

I need you to audit the entire codebase and fix these specific issues immediately:

Hardcoded / Localhost APIs: You are likely still using localhost or Express-based routes for critical functions. Since Express isn't running on the VPS, every call to Withdrawals, Top Players, and Live Withdrawals is returning a 404. You must switch all these calls to the PocketBase SDK or the correct https://api.webcod.in endpoints.

Score Deposit Failure: The game logic is running, but the scores are NOT depositing into Power Tokens. Check the bridge between the game's postMessage and the React Native SCORE_UPDATE function. There is a disconnect here—either the field names don't match or the function is calling a non-existent API.

Withdrawal & Top Players (404): Clicking the Withdrawal button or opening Top Players results in a 404 error. This means the collection names or the query routes in your React Native code do not exist on the production server.

Account Deletion API: The 'Delete Account' function is failing. Verify exactly which API you are calling for this. If it's a custom Express route, it will never work. It must be handled directly via PocketBase.

Live Data Sync: No data is showing in Live Withdrawals or Top Players. You have full access to the database—check why the app is failing to fetch these collections.

Strict Instructions:

Stop assuming the server is at fault. My SMTP and Database are live and verified.

Audit every single directory and file for hardcoded IP addresses or 'Express' dependencies that don't exist in production.

Ensure every single field name matches the underscores (_) exactly as discussed.

I want a build where Withdrawals, Top Players, and Token Claims work perfectly with the production API.

Do not send me another build until you have manually verified that these API paths are correct for a VPS environment and not a local dev environment."
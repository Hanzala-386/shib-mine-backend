Hey Agent,

We have officially purchased and onboarded our FIRST game out of the 6-game arcade hub: "Flappy Bounce". I have provided you with the complete production files: `index.html`, `script.js`, `style.css`, and `icon.png`. 

Now, you need to integrate this game into our existing app architecture using the asynchronous score-matching PvP multiplayer engine we discussed, while applying a specific user-flow and calculation engine.

### 🔄 1. THE GAME CHANNELS & USER FLOW
When a user launches the app, they have two core entry points: Solo Play and Multiplayer Hub. You need to map "Flappy Bounce" into the Multiplayer Hub as follows:
1. Icon Display: Display the provided `icon.png` in our main Arcade/Multiplayer Hub menu.
2. Entry Gate: When a user clicks the Flappy Bounce icon, open the entry-fee selection screen (reusing our existing Power Token / Hit Ticket entry logic).
3. The Matchmaking Screen: Once the entry fee is selected, the user enters the queue looking for an opponent via Socket.io.
4. 🕹️ Offline/Practice Mode Option: On the matchmaking screen, add a clean button for "Play Offline / Practice". If clicked, it bypasses the socket room, launches the raw HTML5 game locally, and lets them play a standard session. Crucial: Offline scores must NOT be written to the database or award any tokens.

### 📊 2. THE MULTIPLAYER CALCULATION & GAME-OVER ENGINE (NO TIMER)
Once an opponent is found, the server triggers `MATCH_START` and opens independent parallel sessions on both devices. For this specific game, do NOT include a match timer. The game runs purely until it hits these criteria:

* Case A (Early Termination / The Chase Rule): Player 1 crashes and gets 'PLAYER_OUT' with a locked final score of 20. Player 2 is still alive at 15 points. The exact millisecond Player 2 scores a hit and reaches 21 points (Player 1's score + 1), the server must instantly trigger `FREEZE_INPUT` on Player 2's client, terminate the match, and declare Player 2 the winner.
* Case B (Standard Both-Out): Both players crash naturally. The player with the higher authoritative score logged on the server is declared the winner.
* Case C (Tie Game): If both players crash out with the exact same score, trigger a Draw and automatically refund their staked entry tokens/tickets via PocketBase.

### 🛠️ 3. TECHNICAL INTEGRATION STEPS FOR YOU
1. Secure Server-Authoritative Session: Lock the score injection. Do NOT allow the game client to write rows to PocketBase directly. The client can only emit scores over the live WebSocket match token wrapper.
2. Hook Injection: Inspect the attached `script.js` (specifically look at `resetGame()`, `resetScore()`, and the death/game-over states where lives hit 0 or the bird hits an obstacle). Inject our standardized `arcade-sdk.js` triggers to broadcast live scores to the server and handle the remote `FREEZE_INPUT` signal cleanly.
3. Suspicious Users Trigger: Keep the anti-cheat monitors active. If a player triggers the "same-score loop" or an impossible score-per-second spike during PvP, immediately siphon their email and metadata into the `suspicious_users` collection for one-click admin banning.

Acknowledge that you have analyzed these specific files (`index.html`, `script.js`) and confirm how you will write the 5-10 lines of adapter code into the game's death sequence to manage this custom calculation flow!
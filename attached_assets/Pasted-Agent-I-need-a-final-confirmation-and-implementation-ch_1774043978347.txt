Agent, I need a final confirmation and implementation check on the Ads Strategy, SDKs, and Asset integration. Please answer each point clearly and complete any pending tasks:

1. SDK Installation & Setup:

Have you successfully installed the SDKs for AdMob, Unity Ads, and AppLovin within the Android/iOS project?

I must not need to manually install any libraries in Android Studio. Ensure all dependencies, manifest permissions, and Gradle settings are pre-configured and 'Build Ready'.

2. Global Banner Ad Logic (Menu Bar):

Implement a Permanent Banner Ad just above the bottom menu bar.

Logic: It must rotate between AdMob, Unity, and AppLovin one-by-one.

Refresh Rate: Auto-refresh every 30 seconds.

Visibility: This banner must stay visible on ALL screens (Home, Game, Profile, etc.) without disappearing.

3. Game Over Ads (Rewarded & Interstitial):

2X Reward Button: When clicked, show a 30-second Rewarded Video. Rotate between all 3 networks. Upon completion, double the Power Tokens.

Claim Button: When clicked, show an Interstitial Ad from any of the 3 networks (one-by-one).

4. Homepage & Specific Action Ads:

For these specific buttons: Start Mining, 2x/4x/6x/10x multipliers, Withdraw button, and Referral Claim button.

Requirement: Show ONLY Unity or AppLovin Interstitial Ads.

CRITICAL: DO NOT show AdMob ads on these specific buttons. Use only Unity/AppLovin rotation here.

5. Profile Page Layout:

Insert Banner Ads between the sections (Referral Reward, My Profile, Preference, App Info).

Use the same 3-network rotation logic as the main banner.

6. Database-Driven Ad IDs (Anti-Crash Logic):

The App must fetch Game IDs and Placement IDs directly from the PocketBase database.

Anti-Crash: If a specific Ad ID is missing or empty in the database, the app MUST NOT CRASH. It should simply skip that network and show an ad from the next available network.

7. Image & Branding Confirmation:

I have provided the branding image. Confirm if you have successfully integrated it as the App Icon and on the Splash Screen.

If the image is still not showing or there is a path error, fix it now. I need to see the 'Shiba Hit' logo when the app starts.

Please provide a status report on these 7 points. If any SDK is not installed or any ad logic is missing, implement it now so the APK can be built perfectly.
CRITICAL REQUIREMENT: We want to overhaul the UI/UX of our main app screen to make it look premium, 3D, and highly animated.
DO NOT modify, break, or alter any existing backend logic, state variables, mining calculations, or wallet balance functions. All current functionalities are working perfectly. This is STRICTLY a frontend UI/UX upgrade.

Please evaluate our codebase and implement the following premium UI changes using standard React Native animation libraries (like react-native-reanimated, Lottie, or layout animations):

1. Central Mining Circle Overhaul:
   - Current: It is a simple flat loop with an "IDLE" text.
   - Upgrade: Transform this into a high-tech 3D-feeling orbital or particle ring. 
   - Idle State: Add a subtle, pulsating neon glow or breathing floating animation.
   - Active Mining State: When mining starts, trigger a continuous, smooth 3D spinning or energy-flow animation (e.g., rotating gradient rings, floating energy particles) that visually represents power accumulation.

2. Interactive Buttons & Tap Physics (3D Feel):
   - Upgrade the "Start Mining" button and the Booster boxes (2x, 4x, 6x, 10x) with inner shadows, 3D pill-shaped bevel effects, and micro-interactions.
   - On Touch/Press: The buttons must scale down slightly (spring contraction) and bounce back up smoothly on release, giving a realistic 3D tactile press feel.
   - Add a premium haptic or subtle mechanical click visual feedback on press.

3. Sleek Typography & Dark Premium Glows:
   - Use dynamic glow filters (shadows with high radius) behind the User Token Display (e.g., 64805 PT) and Booster cards to separate them from the background.
   - Smooth out the "Live Withdrawals" ticker section at the bottom with a neat fading layout transition.

Can you perform this complete visual transformation cleanly while ensuring that all underlying state actions (`onPress` handlers, timers, token accumulation loops) remain exactly the same? Please verify if the required packages for advanced animations are ready or if we need to install specific dependencies first.
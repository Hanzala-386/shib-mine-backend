CRITICAL PRODUCTION EMERGENCY: LIVE REFERRAL LEAK VERIFIED & WEB SIMULATION FREEZE

Hey Agent,

Stop arguing about what you think is fixed. I just ran a live production test, and the gameplay-to-shiba referral leak is 100% ACTIVE right now on the live server, draining assets. We CANNOT update the APK, so you must stop this directly from the backend/PocketBase configuration IMMEDIATELY.

HERE IS THE EXACT PROOF FROM THE LIVE DATABASE:
- User 1: `hamzasha4288@gmail.com` played the game and scored 120 Power Tokens.
- User 2: `gemiko5312@powpearl.com` (the referrer) INSTANTLY received 12 real Shiba Inu Coins (10% of the game score) in their wallet!

This means your Express/Railway route or PocketBase collection hook is still actively processing gameplay scores and awarding commissions.

STRICT INSTRUCTIONS FOR IMMEDIATE FIX (NO APK UPDATE):
1. HARD-STOP FROM POCKETBASE: 
   - Go directly into the PocketBase admin panel/database hooks. If the incoming request or event log originates from any game rewards collection or game payout trigger, BLOCK or intercept the transaction from adding balances to the referrer's profile.
   - Force the database schema or collections to completely reject or return 0 for any referral calculation that does not explicitly carry a `mining_session` origin tag. Lock this channel right now!
2. MINING ONLY: 
   - Re-verify that ONLY the 10% commission on actual mining session claims is permitted natively on the server.

---

NEW ISSUE: "SIMULATE ON WEB" LOADING FREEZE (SEE IMAGE_64BB1A.PNG)
Look at the workspace logs for the web simulator path. When we try to boot the application on the web canvas, it completely freezes on the splash screen showing a non-stop loading spinner with the message "Your app is starting...". 
- This indicates an unhandled promise rejection, a missing environment variable placeholder, or a blocked initialization network call crashing the bootstrap sequence in AdContext/App root when simulated on web. 
- Fix this bootstrap initialization freeze so the web canvas renders the application properly.

Execute the PocketBase/Backend referral block immediately to save our token reserves, and patch the web simulation boot crash right after!
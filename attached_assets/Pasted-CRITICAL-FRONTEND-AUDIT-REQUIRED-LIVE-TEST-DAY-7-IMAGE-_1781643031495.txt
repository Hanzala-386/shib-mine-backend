CRITICAL FRONTEND AUDIT REQUIRED: LIVE TEST DAY 7 IMAGE BUG USING PROVIDED TEST ACCOUNT

The image asset updates successfully in the PocketBase database record, but it remains completely blank/black on the user frontend interface inside the Day 7 Grand Reward box. This rendering issue is ruining the app's premium appearance.

You must stop guessing, log into the frontend live app using the client test account credentials below, look at the blank container yourself, and fix the dynamic URL path rendering logic.

TEST ACCOUNT CREDENTIALS FOR AGENT:
- Email: sakibsoni27@gmail.com
- Password: 12345678

EXACT STEPS FOR AGENT TO IMPLEMENT AND VERIFY:

1. UNDERSTAND THE FRONTEND RENDERING FAILURE:
   - For Day 1 to Day 6, your mapping logic is correctly pulling images from the database and displaying them.
   - For Day 7, the array map or conditional block (e.g., `day === 7`) is broken. It is either looking for the wrong field name (e.g., mixing up `day_7_shiba_image` and `day_7_power_image`) or failing to generate the correct PocketBase file view URL string.

2. MANDATORY CODE CORRECTION:
   - Go directly into the Daily Rewards modal frontend component file.
   - Locate the layout block that renders the Day 7 wide container card.
   - Force-inject a proper HTML/React image element (`<img src="..." />`) inside this container. 
   - Ensure the image `src` correctly points to the static asset folder or resolves the full authenticated PocketBase URL path for the Day 7 image field.
   - Ensure the image is set to fill the wide card responsively with custom properties to fit the 1000x250 aspect ratio design.

3. VERIFICATION LOG:
   - Once you update the file, check the app view using the test account. If Day 7 is still rendering as a flat blank black box, your code is still wrong. 
   - Confirm in your response that the banner is now 100% visible on screen for user sakibsoni27@gmail.com.

Fix this rendering path glitch right now. Do not touch any backend rules or other working logic—just fix the frontend image source visibility for Day 7.

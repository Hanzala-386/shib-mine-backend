CRITICAL PRODUCTION APK BUG FIXES: IMAGE UPLOAD NETWORK FAILURE, TEXT REFACTOR, AND DAILY CLAIM REWARD SYNC

We have generated the standalone production APK build and completed testing. We encountered three distinct bugs that need immediate resolution based on the provided screenshots.

---

### BUG 1: PROFILE IMAGE UPLOAD FAILS ON PRODUCTION APK ("Network request failed")
- Reference File: "1000436412.jpg"
- Issue: In the Expo Go / development preview env, the profile picture upload functions perfectly. However, in the standalone release APK, attempting an upload triggers an "Upload error: Network request failed" modal.
- Required Fix: 
  1. Ensure that the multi-part Form-Data payload wrapper explicitly binds the correctly formatted local file URI structure (`Platform.OS === 'android' ? uri : uri.replace('file://', '')`) alongside a default fallback MIME type configuration (`image/jpeg`).
  2. Audit `app.json` or `AndroidManifest.xml` configurations to guarantee that Android Network Security Configs permit network access hooks for binary uploads and multi-part data pipelines.

---

### BUG 2: WITHDRAWAL TEXT UPDATE FOR CLEAR CLARITY
- Reference File: "1000436610.jpg"
- Issue: The subtext label below the BEP-20 withdrawal lock currently states: "This withdrawal method unlocks automatically once your balance exceeds 50,000 coins."
- Required Fix: Refactor the hardcoded UI string component inside the Wallet/Withdrawal view layer. Update the text to state exactly:
  "BEP20 withdrawal method unlock automatically once your balance exceed 50000 coins."

---

### BUG 3: DAILY REWARDS DESYNC FROM DATABASE VALUE (HARDCODED FALLBACK ISSUES)
- Reference File: "1000436614.jpg"
- Issue: As seen in the screenshot, Day 3 displays a standard '200 SHIB' structure on the calendar grid block. However, clicking the button returns a hardcoded default reward allocation of '+3K SHIB' in the active top status panel alert. The client application is bypassing dynamic database checks.
- Required Fix: Audit the claim handler controller function for Daily Rewards. The engine must absolutely STOP reading old default fallback configurations built during testing phases. Ensure that the logic actively reads the dynamic value assigned inside the PocketBase database configurations for that active day node, so that user payouts exactly match what is rendered in the UI grid.

---

Execute these refactoring iterations directly into the codebase right now. Let me know once complete so we can rebuild the testing preview package.

CRITICAL FRONTEND UPLOAD BLOCK: REPLICATE WITH NEW ACCOUNT AND FIX MULTIPART/FORM-DATA BUG

The database check works when you upload files manually via the PocketBase backend admin panel, but the actual React Native / mobile client submission pipeline is BROKEN. 

Watch the app state carefully: When a user selects a screenshot and taps "Submit", the loading spinner rotates briefly, fails silently, and returns to the unsubmitted "Submit" state without appending any new record to PocketBase.

You must run a direct runtime injection test right now using this second test account provided by the user:
- Email: abukapadi@gmail.com
- Password: Hanzu@123

---

DIAGNOSTIC & FIX PROTOCOL FOR AGENT:

1. THE ROOT CAUSE (MULTIPART PAYLOAD ISSUE):
   - Check your submission function handler inside the frontend. When uploading images to PocketBase, you cannot send a raw JSON body. It must be wrapped correctly within a `FormData()` instance.
   - Ensure the structure matches this exact format:
     ```javascript
     const formData = new FormData();
     formData.append('user_id', currentUserId);
     formData.append('task_id', currentTaskId);
     formData.append('status', 'under_review');
     formData.append('proof_image', {
       uri: platformUri, // Ensure file:// protocol formatting for Android/iOS
       name: 'proof.jpg',
       type: 'image/jpeg',
     });
     ```
   - Verify that your headers do NOT conflict. Let your fetch/axios instance automatically assign the `multipart/form-data` boundary header instead of hardcoding it incorrectly.

2. RUN THE TEST:
   - Authenticate with the credentials: abukapadi@gmail.com / Hanzu@123.
   - Try uploading a valid image to the active Instagram task from the app view interface.
   - Inspect the exact network response code. If PocketBase returns a `400 Bad Request` or `443 client error`, it means your schema field name doesn't match the form-data key or the file URI format is corrupted during selection.

Fix this API carrier function immediately so that users can smoothly upload proof files directly from their mobile app screens. Confirm once the test row shows up properly in your database.

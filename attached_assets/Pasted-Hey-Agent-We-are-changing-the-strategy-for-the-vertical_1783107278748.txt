Hey Agent,

We are changing the strategy for the vertical slice. We want to test your absolute maximum capability, engineering power, and rendering limits right from Day 1. Instead of starting with a simple game, we are starting with the most technically challenging title in our catalog: **8-Ball Pool**.

For this to succeed, you must precisely handle the 2D top-down deterministic physics engine (ball collision, angles, momentum, cue speed), sound trigger events, smooth line-aiming trajectories, and 60 FPS Skia rendering. If you can deliver this flawlessly, we know the rest of the platform will be a breeze.

Here is the exact Navigation and Economy structure we need you to implement for this initial release:

### 1. New Home Screen UI Flow (The Game Hub Entry)
* Currently, tapping the bottom game section launches a game immediately. Change this completely.
* When a user taps the Game section, display a split interface/two prominent option cards:
  1. Solo Play (Knife Hit Game - for earning Power Tokens)
  2. Multiplayer Tournament Hub
* When the user taps "Multiplayer Tournament Hub", it opens the main lobby where our 6 games will live. For now, only **8-Ball Pool** will be active.

### 2. 8-Ball Pool Multi-Tier Lobby Setup
When the user clicks on 8-Ball Pool, open a matchmaking tier selection screen. The tiers will range strictly based on Power Tokens (PT):
* Entry Fee Tiers: Minimum 1,000 PT up to Maximum 100,000 PT (e.g., 1k, 5k, 10k, 50k, 100k PT rooms).
* Users choosing the same tier must be dynamically paired via the raw-websocket matchmaking engine. 

### 3. Wallet Updates & Core Math Verification
We are introducing a permanent secondary currency wallet asset: **Hit Tickets**.
* UI Requirement: Design a clean, premium, compact "Ticket" icon/symbol for the ledger and header bars.
* The Economics Formula to program into the server-authoritative ledger:
  * Baseline Conversion: 1,000 PT = 10 Hit Tickets value pool per player.
  * Entry: Both players stake equal PT based on the room tier. (e.g., 1,000 PT each $\rightarrow$ Total Pool = 2,000 PT value $\rightarrow$ 20 Total Tickets).
  * Platform Commission: 10% strictly deducted by the backend upon match settlement.
  * Winner Payout: The winner of a 1,000 PT match receives exactly **18 Hit Tickets** credited to their wallet balance. 

### 4. Loyalty Redeem Page Layout
Build the dedicated "Redemption Center" page where users can cash out their Hit Tickets into Shiba Inu (SHIB) cryptocurrency:
* Fixed Rule: 1 Hit Ticket = 10 SHIB Coins.
* Create a list of multi-tier redeem box components (ranging from a minimum of 50 tickets up to a maximum of 5,000 tickets per transaction). Tapping a box checks the user's `hit_tickets` balance, debits the tickets via an idempotent server write, logs a pending withdrawal transaction, and updates the UI.

### Live Testing Notice:
We will be opening the app on two separate active devices with different User IDs simultaneously to live-test your matchmaking queues, real-time sync, physics consistency across screens, and the automated token-to-ticket wallet payouts.

Please skip the design canvas phase entirely. Move straight into coding. Stand up the required database schemas for the new ticket ledgers/redeem tables, configure the UI navigation split, and build the core 8-Ball Pool authoritative engine. Show us the full code implementation now!
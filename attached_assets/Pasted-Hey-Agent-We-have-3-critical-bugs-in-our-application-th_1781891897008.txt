Hey Agent,We have 3 critical bugs in our application that are breaking the core gameplay, security, and monetization. Look at the attached screenshots (WhatsApp Image 2026-06-19 at 11.21.30 PM.jpeg, image_210ed8.png, and edited-image_2.png) and fix these issues permanently right now:1. Fix Tournament Points Desync (Database showing 0 points)The Issue: In the UI (WhatsApp Image 2026-06-19 at 11.21.30 PM.jpeg), users like Leo (1K pts) and Abu (113,998 pts) have active tournament points. However, in our PocketBase tournament_participants collection (image_210ed8.png), the points column for EVERY single user is stuck at 0. If the DB stays 0, the automated lifecycle runner will fail to decide the winners at the end of the week.  The Fix:Check the frontend tap/mining action. It is updating the local UI state or maybe a different collection, but it is NOT performing an API patch/update request to the points field of the tournament_participants collection for the active week_start.  Ensure that whenever a user earns points (tapping or booster actions), an immediate or batched API call triggers: PATCH /api/collections/tournament_participants/records/:id to increment the points field in the database.  2. Fix Auto-Clicker Detection FailureThe Issue: In edited-image_2.png, you can clearly see a prominent third-party floating Auto-Clicker overlay running right on top of our game screen. The application completely failed to detect, block, or stop it, even though we integrated anti-cheat systems.The Fix:Re-verify the Android/Flutter/React Native background channel logic for package detection and accessibility service monitoring.Implement a strict check on launch and during active gameplay loops that queries active accessibility services or checks for common auto-clicker package names (e.g., com.truedevelopersstudio.automatictap.autoclicker).If detected, immediately trigger a blocking overlay modal stating: "Auto-Clicker detected. Please turn off or uninstall the tool to continue playing." and force-stop the mining session.3. Fix Forced Unity Ads Not Displaying (Google Ads Overriding)The Issue: Even though we explicitly set the admin config or code to "Force Unity Ads", the app is ignoring this instruction and still continuously showing Google AdMob ads.The Fix:Go to our global ad controller manager/utility script. There is a logical flaw or hardcoded switch causing AdMob to take precedence.Make sure the ad placement function strictly evaluates the forced switch: If forced_network == 'unity', it must bypass the AdMob initialization/loading flow entirely and trigger the Unity Ads Reward Video SDK instance directly.Review the attached images, trace these data flows, clean up the logic, and push the fix immediately. Let me know when it's done.
URGENT: Final Audit & Logic Implementation Required (Action Required)
"Hi,

I have attached a video and several screenshots showing that the app is still failing in key functional areas. I have also provided a new SMTP/Brevo key for the account deletion system. Please update the code based on these 4 critical points:

1. Claim Button & Fraud Logic (UX Loop): [Ref: Attached Video]

In the video, you can see the 'Claim' button is clicked multiple times, but nothing happens. The UI just stays on 'Claiming...'.

Requirement: Even if it's a fraud attempt (time manipulation), the button MUST complete its action. The app should call the backend, the backend should REJECT the tokens, and the app should show a 'Claim Rejected' message.

Do NOT freeze the screen. Increment the fraud_attempts and let the user return to the home screen to start a new mining session. We need to track these 3 attempts before a permanent block.

2. Delete Account - SMTP/Auth Failure (401): [Ref: Attached Screenshot]

I am getting a 401 Unauthorized error when sending OTP. This is an API Header issue.

Action: I have generated a NEW Brevo Key. Use the following credentials and ensure they are passed correctly in the REST API headers (api-key):

SMTP Server: smtp-relay.brevo.com

Port: 587

Login: a52a39001@smtp-brevo.com

New SMTP Key: xsmtpsib-57d87a3812ae04a7addce247e7bb94c093e2fbc9e18524fdd25eced8f3762011-N9VMePFXzvROgxUP

Flow: Send OTP -> Open 6-digit input box -> Delete user -> Move email to deleted_emails collection.

3. Live Withdrawals Ticker (Missing Section):

The 'Live Withdrawals' section under Top Players has disappeared.

Action: Restore the right-to-left scrolling ticker. It should fetch the latest 10 'Approved' withdrawals from the database. If no new ones exist, it should continue looping the last 10.

4. Referral Rewards Mismatch: [Ref: Profile Screenshot]

The top counter shows '1 Referral', but the list below says 'No referrals yet'.

Fix: The list must correctly fetch and display the emails/usernames of the users who signed up using the referral code. Ensure the referrals collection is being queried properly for the logged-in user's ID.

Note to Agent: Please watch the video carefully to understand the Claim button freeze. Use the new SMTP key provided above to fix the 401 error. I expect a build where all these flows are synced with the live database."
Urgent: PocketBase Schema Audit & System Fixes Required
"Hi,

The game UI is working, but the core app logic (Tokens, Claims, Auth, and Boosters) is completely broken. We are seeing multiple HTTP 404 errors and data sync issues. I have audited the system and here is the list of exact fields and systems you need to fix in PocketBase immediately.

Please Ensure: Field names must be EXACT (use underscores _ as specified). Do not use CamelCase or combined names.

1. Collection: users (Essential for Auth & Scoring)
The following fields are either missing or have naming mismatches:

firebase_uid, referred_by, total_claims, otp_code, otp_expires, is_verified

active_booster_multiplier, booster_expires, referral_earnings

last_session_score, total_accumulated_score, collected_tomatoes

referral_balance, current_mining_session, fraud_attempts, status

Issue: New users cannot register because the email/OTP system is failing.

2. Collection: mining_sessions (Claiming logic is broken)
The tokens are not adding up after claiming because of these fields:

start_time, claimed_amount, ip_address, booster_multiplier

3. Collection: settings (Global Config)
The app cannot fetch rewards/costs because of these fields:

mining_rate_per_sec, power_token_per_click, boost_2x_cost, boost_4x_cost, boost_6x_cost, boost_10x_cost

tokens_per_round, mining_duration_minutes, play_store_url, rate_popup_frequency

4. Other Missing/Broken Collections:
daily_usage: fields: date_day, count

deleted_emails: field: email

otp_codes: fields: user (relation), code, expires_at

withdrawals: field: status

Critical System Failures to Fix:
Email/SMTP Server: Registration and 'Delete Account' emails are not being sent. Please configure the SMTP settings in PocketBase so OTPs can be delivered.

Field Naming Convention: Check if you have removed underscores (e.g., using starttime instead of start_time). The App code expects the underscore version. Update the schema to match the code exactly.

Localhost vs Production: If any logic is still pointing to localhost or a hardcoded test IP, it will fail. Ensure all API calls are pointing to https://api.webcod.in.

Claim Logic: When a user clicks 'Claim', the total_accumulated_score in the users collection must be updated based on the last_session_score. This is currently not happening.

Note: The game logic on shared hosting is sending the SCORE_UPDATE correctly, but the React Native bridge is failing to save it because the PocketBase fields don't match.

Please fix these schema names and the SMTP server immediately so we can test the full flow."
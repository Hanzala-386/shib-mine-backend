We want to implement a complete "Live Support Ticket System" with a floating widget in our React Native mobile application and link it to our existing PocketBase backend and Admin Panel. 

CRITICAL: Do not modify, alter, or break any existing features, database logic, or screens. All old systems must remain 100% operational. This is strictly an incremental new feature.

Please implement the following architecture across the project:

1. PocketBase Collection Setup:
   - Create a new collection named `support_tickets` with fields:
     - `user_name` (text, required)
     - `user_email` (text, required)
     - `question` (text, required)
     - `reply` (text, optional/nullable)
     - `status` (text/select: values 'Pending' or 'Replied')
     - `is_read_by_user` (bool, default: true) - This turns false when a admin replies, to trigger the notification badge.

2. Mobile App (React Native Frontend):
   - Floating Widget: Add a floating, circular Support Icon overlay on top of all screens. It must use PanResponder or react-native-gesture-handler so users can smoothly drag it anywhere on the screen. On release, make it snap magnetically to the nearest left or right edge.
   - Unread Badge: If a ticket has `status == 'Replied'` and `is_read_by_user == false`, display a small, modern Red Dot Badge (Notification Indicator) on the top-right corner of the floating circle.
   - Support Modal: Clicking the floating icon opens a clean dialog modal:
     - If the user has a pending question, show: "Your question is under review. We will reply within 12 hours."
     - If the admin has replied, show the original question, the admin's response, and a "Clear / Start New Ticket" button (which resets the view and changes `is_read_by_user` to true).
     - If no active ticket exists, show a text input field for the question and a "Submit Support Ticket" button.

3. Admin Panel Page Setup:
   - Add a new sidebar menu item and page named "Live Support".
   - Fetch records from `support_tickets` where `status == 'Pending'` or layout them cleanly in tabs.
   - Display a table containing columns: User Name, Email ID, Question, and an inline Text Input field for "Reply" alongside a "Send Reply" button.
   - When the admin submits a reply:
     a. Update the row: Set `reply` text, change `status` to 'Replied', and toggle `is_read_by_user` to false.
     b. Ensure it updates via PocketBase realtime subscription or clean api trigger so the user's mobile app immediately displays the red dot badge.

Please implement this complete flow, manage file structure neatly, and ensure full stability before marking it complete.
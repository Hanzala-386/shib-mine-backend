FINAL MASTER INSTRUCTION: STABLE MINING FLOW (BASED ON PB_SCHEMA)
STRICT DIRECTIVE: I have provided my exact PocketBase schema. You must fix the mining flow so it is 100% reactive and server-synced. No more logout/login loops.

1. Database Fields to Use (Strictly):
Collection: users

Fields: power_tokens (Number), active_booster_multiplier (Number), booster_expiry (Text/DateTime).

Settings: Get costs from settings collection (e.g., booster_10x_cost).

2. The Mining Logic Flow:
When any booster button (2x, 4x, 6x, 10x) is clicked:

Validation: Compare user.power_tokens with settings.booster_X_cost.

Insufficient Balance: Show a Popup: "Insufficient power tokens! Please play the game." with a "Play Game" button (Redirect to /games).

Sufficient Balance: Show a Confirmation Popup: "Start [X]x Mining?"

Activation (The Fix): - Update users collection: Set active_booster_multiplier to X, deduct power_tokens, and set booster_expiry to exactly 1 hour from now (new Date(Date.now() + 3600000).toISOString()).

CRITICAL: Use pb.collection('users').subscribe in MiningContext so the UI sees this change INSTANTLY. Do not make the user logout.

Timer: The timer must always calculate: TimeLeft = booster_expiry - CurrentTime.

3. Claim Button Rules:
The 'Claim' button must be HIDDEN as long as booster_expiry is in the future.

Show 'Claim' only when booster_expiry <= Now.

Once claimed, reset active_booster_multiplier to 1 and clear/update the session so the button disappears even after logout/login.

4. Multi-Account Testing (Insert Details):
Test this logic using these accounts to ensure no crashes or NaN values:

[TEST 2X] - Email: [sakibsoni27@gmail.com] | Pass: [12345678]
[TEST 4X] - Email: [jetrocompressor@gmail.com] | Pass: [12345678]
[TEST 6X] - Email: [poolshaikh88@gmail.com] | Pass: [12345678]
[TEST 10X] - Email: [sakoxif153@ramynd.com] | Pass: [Hanzala123]

FIX THE "BIND" ERROR: Ensure AsyncStorage is only called inside useEffect in WalletContext.tsx to stop the preview from crashing.
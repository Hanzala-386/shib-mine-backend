CRITICAL SECURITY BUG AUDIT REQUISITION: TEST LIVE AND EXPLAIN THE DATABASE DISCONNECTION AND CLOCK EXPLOIT IMMEDIATELY

We are auditing the "Daily Rewards" system you just updated, and it is completely broken both visually and structurally. Look at the database right now: the "daily_claims" collection is completely empty (No records found) even after a user attempts a claim. Furthermore, the timer is glitching and showing absurd values like 48+ hours, and the UI still looks like an outdated basic app instead of a premium asset-based gaming design.

Before you touch any code, you must manually log in, investigate these critical issues, and answer our specific questions.

YOUR MANDATORY TESTING CREDENTIALS:
- User Email: abukapadi@gmail.com
- User Password: Hanzu@123

STEP 1: PERFORM THIS LIVE TEST RIGHT NOW
1. Log into the application using the credentials provided above.
2. Open the Daily Rewards popup and observe the broken timer and plain UI.
3. Now, go to your mobile phone or emulator settings, manually advance your device clock by 2 days, and return to the app.
4. Notice how the app instantly allows you to bypass the rules and claim subsequent days! 

---

ANSWER THE FOLLOWING QUESTIONS IN DETAIL BEFORE PROCEEDING:

QUESTION 1: Why is a user able to bypass the 24-hour limit simply by changing their phone's manual time? Explain exactly why your current code is trusting the client-side device clock instead of strictly validating the elapsed time against a trusted server clock/timestamp.

QUESTION 2: Is it strictly possible to implement a bulletproof server-side clock sync (matching professional network time protocols) so that no matter what the user does to their mobile phone settings (advancing days, changing timezones, forcing local overrides), the "CLAIM NOW" button remains 100% locked until the actual server backend database confirms 24 hours have passed? Confirm if you can guarantee a 100% unhackable server-time lock.

QUESTION 3: Why is the "daily_claims" database collection completely empty with "No records found" when a user triggers a claim? Explain the disconnection between your frontend execution and the backend schema storage.

QUESTION 4: Why are the dynamic images uploaded in the admin settings panel (e.g., day_1_image, day_2_image) not rendering correctly within the grid boxes, and why is the text amount not sitting cleanly below those custom images as specified?

---

STRICT MANDATE: Do not write random code blocks. Log in using the credentials above, replicate the clock hack yourself, check the empty database collection, and reply with clear technical explanations for the questions above. If you are fully capable of fixing this time-exploit via absolute server-side locking, state your complete implementation plan.

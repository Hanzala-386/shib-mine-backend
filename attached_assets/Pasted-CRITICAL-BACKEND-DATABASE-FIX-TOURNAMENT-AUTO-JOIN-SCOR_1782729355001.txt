CRITICAL BACKEND/DATABASE FIX: TOURNAMENT AUTO-JOIN & SCORE MISMATCH (NO APP UPDATE ALLOWED)

Hey Agent,

We are facing a critical production issue with how the tournament lifecycle is interacting with the mobile app. We CANNOT push a new APK update right now, so whatever you fix MUST be done strictly server-side, within the backend logic or PocketBase configurations. 

Lately, some changes were made to the database architecture and admin panel hooks regarding the `tournament_participants` cleanup. Because of those database modifications, the legacy application logic has broken in production.

HERE ARE THE CRITICAL ISSUES YOU NEED TO FIX NATIVELY FROM THE BACKEND:

1. AUTOMATIC JOINING BUG (MISSING REGISTRATION GATE):
   - As soon as a new tournament is launched, users opening the app are being automatically registered/joined into the tournament instead of seeing the mandatory "Register/Join" notification screen. 
   - I have launched a test tournament cycle in the admin panel right now. Turn it ON, test the API responses from the backend, and find out why the registration overlay prompt is being completely bypassed on the current live app version.

2. HISTORICAL SCORE LEAK (SCORES NOT RESETTING TO ZERO):
   - When users are getting auto-joined, their tournament score is NOT starting from zero. Instead, the mobile app is displaying massive historical scores (like 150,000, 100,000, etc.) fetched from old tournament data.
   - Interestingly, the Admin Panel's participation table shows the scores correctly, but the app is somehow picking up and caching old data from the history context. You need to ensure the backend API strictly serves a fresh `0` balance/points baseline for the active cycle without requiring an app update.

3. THE BACKGROUND CONTEXT:
   - In the previous cycle, the `tournament_participants` table failed to clear out properly. We had to manually force-clear the table data to keep the database running. This manual database manipulation is what broke the app's sync logic.

STRICT INSTRUCTION: Do NOT alter the frontend mobile application code. Do not suggest a new build. Diagnose the current live database states, check why the test tournament fails the registration gate, and fix the backend database pointers so everything resets perfectly to zero and requests the registration prompt natively. Let me know once the backend fix is deployed and verified on the test cycle.
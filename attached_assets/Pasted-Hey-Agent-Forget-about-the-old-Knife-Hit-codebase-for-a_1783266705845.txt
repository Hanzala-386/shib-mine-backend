Hey Agent, 

Forget about the old Knife Hit codebase for a moment—do not touch or reference its current structure. We are shifting to a fresh strategy. We want to build a clean Asynchronous Score-Matching PvP Multiplayer Engine for SIX (6) brand-new HTML5/Canvas games that we are buying from CodeCanyon. 

### 🕹️ 1. MULTIPLAYER CORE LOGIC & GAMEPLAY FLOW
The 6 games will be standalone HTML5 games. Examples of the titles we are onboarding include:
1. Color Rush (HTML5 Arcade)
2. Boon 2048 (HTML5 Puzzle/Merge)
3. Aqua Rescue (Canvas Arcade)
4. Flappy Bounce (Side-Scroller Arcade)
...and 2 more similar score-based arcade titles.

The multiplayer engine must work exactly under these strict operational rules:
* Parallel Independent Play: Both paired players play simultaneously on their own individual screens. No shared real-time physics sync is required—only score and state data cross the network.
* Score & Out Calculation: The match is bounded by a strict match timer. The final winner is calculated based on WHO HAS THE HIGHEST SCORE when either the timer expires or both players get 'Out'.
* Early Win Condition: If Player 1 gets 'Out' early with a maximum score of 50, the absolute millisecond Player 2 (who is still alive) hits 51 points, the server must INSTANTLY terminate the match, freeze the inputs, and declare Player 2 the winner. No need to drag the match further.

Can you build this universal, plug-and-play WebSocket/Socket.io wrapper so that any of these 6 HTML5 game folders can be dropped in and automatically inherit this multiplayer logic?

---

### 🚨 2. ANTI-CHEAT FRAMEWORK & CRITICAL FRAUD INCIDENT
Since real assets (Hit Tickets/Tokens) are on the line, security is our absolute priority. We just patched our client where users used to spam the claim button multiple times, but a malicious hacker has bypassed our frontend and is exploiting the app. 

Look at the attached database logs ("WhatsApp Image 2026-07-05 at 9.11.55 PM.jpeg" and "WhatsApp Image 2026-07-05 at 9.11.55 PM (1).jpeg"). This specific user is submitting an EXACT raw_score of "1200" every single day at completely random times. They are freezing client-side memory or intercepting the API payload to spoof this score.

To completely crush this, we need you to design a Bulletproof Server-Authoritative Anti-Cheat and Monitoring System. Please explain how you will implement the following defenses:

A. Payload Encryption & Verification: How can we protect the final score submission from the HTML5 Canvas/WebView? What encryption or secure cryptographic hashing (like HMAC with time-based nonces) will you implement so users cannot forge api score requests?

B. Automated Anomaly Detection & Flagging Rules: We need the server to catch these two exact fraudulent behaviors in real-time:
   1. The Same-Score Spammer: If a user achieves and submits the exact same high score (like this '1200' loop) across multiple games consecutively, the server must flag it as an anomaly.
   2. The Unnatural God-Mode Win Streak: If a user plays 100 to 200 matches and wins 100% of them with mathematically improbable high scores without a single loss.

C. Automated Admin Panel Quarantine Hub ("suspicious_users"):
   - Instead of letting them ruin the economy, the moment the server triggers any of the anomaly flags above, it must INSTANTLY extract that user's email and details and insert them into a brand-new PocketBase collection called `suspicious_users`.
   - This section will act as our admin radar, allowing us to review their logs and completely BLOCK them from the ecosystem with a single click from our admin panel.

Please give me a highly comprehensive, step-by-step breakdown of how you will structurally build this multiplayer engine and how we will trap these hackers. Confirm if you can build all these custom collections and anti-cheat triggers end-to-end!
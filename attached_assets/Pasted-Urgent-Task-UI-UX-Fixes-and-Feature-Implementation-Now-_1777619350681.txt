Urgent Task: UI/UX Fixes and Feature Implementation
"Now that the PocketBase backend is stable and fully connected, we need to address several critical functional issues in the app. Please analyze and fix the following:

1. Withdrawal Tiers Sync Issue:
The withdrawal amounts for Tier 1, 2, and 3 set in the database settings (e.g., 499, 5999, 19999) are not reflecting correctly in the application UI. The app is showing hardcoded or outdated values (like 'Min: 8K SHIB'). Ensure the frontend dynamically fetches and displays these values exactly as they are configured in the PocketBase settings collection.

2. Live Withdrawals Username Bug:
In the 'Live Withdrawals' section, every approved withdrawal currently displays the placeholder name 'Miner' instead of the actual user's username. The logic is working, but the name mapping is wrong. Update this section to fetch and display the specific username of the person who made the withdrawal.

3. Share Button Update & Referral Logic:

App Link: The app is now live on the Play Store. Update the share button's message to include this official link: [https://play.google.com/store/apps/details?id=com.hanzalasha.shibmine](https://play.google.com/store/apps/details?id=com.hanzalasha.shibmine)

Auto-Referral Research: I want to know if we can implement a system where, if a user downloads the app via a shared link, their referral code field is automatically filled during registration. Is this feasible with our current setup (e.g., using Firebase Dynamic Links or Play Install Referrer API), or is it too complex? If it's simple, please implement it; otherwise, just ensure the link is shared correctly.

4. Banner Ad Loading & Refresh Optimization:
We are seeing very low ad fill rates (only 2 out of 10 devices load ads).

Initialization: Optimize the AdMob/Banner Ad initialization so that ads load immediately upon app startup.

Refresh Rate: Implement an auto-refresh logic for banner ads every 30 seconds to maximize revenue.

Performance: Enhance the loading speed of banner ads across all screens where they are placed. It should be consistent on every device.

Constraint: Please decide on the best code implementation yourself. Do not change any established Railway production configurations. Focus on fixing the frontend-backend data sync and ad-delivery logic."
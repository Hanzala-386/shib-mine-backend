Agent, I have manually updated the PocketBase schema. Now, your task is to implement a robust, Server-Side-Only Mining & Anti-Cheat Logic. Follow these strict technical requirements:

1. User Status & Initial Setup:

When a new user is created, ensure their status is set to 'active' and fraud_attempts to 0 by default in your code logic.

Before allowing any action, always check if the user's status == 'active'. If it is 'blocked', deny all API requests and force a logout with a message: 'Your account is permanently banned due to multiple fraud attempts.'

2. Bullet-Proof Mining Start:

When the user starts mining, create a new record in the mining_sessions collection.

Immediately update the current_mining_session field in the users collection with the ID of this new session.

CRITICAL: Use the PocketBase server's created timestamp as the only source of truth for the start time. Ignore the client's phone clock.

3. Anti-Cheat Claim Logic (The Core):

When the user clicks 'Claim', the backend (or your controller logic) must:
a) Fetch the mining_duration_minutes from the settings collection.
b) Fetch the created time of the session linked in the user's current_mining_session.
c) Compare the Current Server Time with the Session Created Time.

Condition: If (Current_Server_Time - Created_Time) is LESS than mining_duration_minutes, this is a FRAUD ATTEMPT (user changed their phone clock).

4. Strike & Ban System:

On a detected fraud attempt:
a) DO NOT grant any rewards.
b) Increment the fraud_attempts field in the users collection by +1.
c) If fraud_attempts < 3: Show a warning popup: "Strike {count}/3! Fraud detected. Changing phone time is not allowed. 3 strikes = Permanent Ban."
d) If fraud_attempts >= 3: Set user status to 'blocked' and terminate the session immediately.

5. Seamless UI Sync:

To prevent honest users from being confused, fetch the server time from PocketBase every 60 seconds to sync the frontend countdown timer.

The 'Claim' button should only be enabled on the UI when the server-synced timer reaches zero, but the final validation must always happen on the server side during the API call.

6. Cleanup:

Once a successful claim is made, nullify the current_mining_session field in the users collection so they can start a new one.

Please implement this logic across the Auth and Mining controllers now.
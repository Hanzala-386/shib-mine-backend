CRITICAL FRONTEND FIXES: TOURNAMENT PARTICIPANTS LIFECYCLE, BUTTON ACTION LOGIC, AND LEADERBOARD INFINITE REFRESH BUG

Hey Agent, 

We need to address 3 severe logical bugs in the Mobile Application's Tournament module immediately. Read the structural requirements below carefully and implement the fixes cleanly:

---

### 1. TOURNAMENT PARTICIPANTS DATA ISOLATION & SESSION CHECK
- THE BUG: Old user records from past tournaments remain cached or verified incorrectly. Because a user's ID exists from an older session inside the dynamic database check, the app bypasses the new tournament prompt, preventing users from seeing the fresh Poster with the Register/Reject action buttons.
- THE IMPLEMENTATION:
  * Do NOT just check if the user exists blindly in `tournament_participants`. You MUST strictly validate if the user has a record tied to the CURRENT active `tournament_id` or matching the current cycle's `week_start` timestamp.
  * LFCYCLE UPDATE: When an active tournament configuration instance hits its `end_time` and expires, the app client must consider the local participant state wiped/reset for the new session. (The backend handles archiving data to history, the frontend must strictly look for a fresh, matching `tournament_id` record row). If no record matches the new ID, immediately trigger the entry poster registration view sequence.

---

### 2. FIX ACTION BUTTON LOGIC ([REGISTER] vs [REJECT])
- THE BUG: Currently, tapping the [REJECT] button accidentally triggers the exact same data insertion sequence as the [REGISTER] button, logging the user as a participant anyway. This is completely wrong.
- THE FIX:
  * [REGISTER] BUTTON: Tapping this must perform a POST to insert the user into `tournament_participants` for the active tournament, and instantly unlock the full Leaderboard/Mining gameplay UI context.
  * [REJECT / CLOSE] BUTTON: Tapping this must simply close the overlay poster view container. It must NOT create any entry rows inside `tournament_participants`. 
  * If a user rejects the initial pop-up, when they manually navigate to the "Weekly Tournament" tab view layout, they should ONLY see a static "JOIN TOURNAMENT" call-to-action button alongside the active countdown time clock (e.g., "Starts In: [Time]" or "Ends In: [Time]"). They must NOT see player point scores or ranking positions until they explicitly hit that manual "Join" button to register.

---

### 3. FIX LEADERBOARD RANK GRID INFINITE REFRESH / LOADING GLITCH
- THE BUG: Once a participant is registered and views the active weekly leaderboard stack, the component fails to load the sorted player ranking rows cleanly. Instead, it gets stuck in a perpetual loading/re-rendering loop, continuously flashing/refreshing the bottom area without showing data rows mapped to user point aggregates.
- THE FIX:
  * Audit the state dependencies inside your `useEffect` hooks or list component rendering layers responsible for displaying the participant scores list.
  * Ensure that the participant collection data is fetched once, sorted locally or via PocketBase query parameters descending by `points` value, and updated cleanly in a single state set operation. Break any circular state changes that are forcing the components to loop-refresh constantly. Show the user's correct calculated ranking directly based on their sorted points position.

---

Refactor these core layout flows and button intercept handlers inside the React Native tournament module immediately. Let me know once compiled and stable.